<%= turbo_stream.replace(@card,
                         partial: "cards/card",
                         locals: { card: @card }) %>

<%= turbo_stream.append(:toasts) do %>
  <%= render 'shared/toast', title: 'Card', toast_class: toast_add do %>
    <p>Atualizado com sucesso!</p>
  <% end %>
<% end %>