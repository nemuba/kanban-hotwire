<%= turbo_stream.replace(@board,
                         partial: "boards/board",
                         locals: { board: @board }) %>

<%= turbo_stream.append(:toasts) do %>
  <%= render 'shared/toast', title: 'Board', toast_class: toast_add do %>
    <p>Atualizado com sucesso!</p>
  <% end %>
<% end %>